# Intel Pin Setup

DiffTrace uses a custom pintool for dynamic execution tracing. Intel Pin is not included in this artifact. Install a compatible Intel Pin distribution separately, then build the pintool under `pintool/` and set the Pin binary path in the experiment configuration.
